apiVersion: v1
kind: Service
metadata:
  name: account-created-public
  namespace: anthosguard
spec:
  type: LoadBalancer
  selector:
    app: account-created
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP