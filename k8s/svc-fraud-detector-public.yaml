apiVersion: v1
kind: Service
metadata:
  name: fraud-detector-public
  namespace: anthosguard
spec:
  type: LoadBalancer
  selector:
    app: fraud-detector
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP